# Dev-Only Local MCP Notes

This document is for local development only.

- Local MCP endpoint is typically: `http://localhost:8000/mcp`
- In the main Direct Alert project, local MCP verification is usually run via:

```bash
make verify-mcp-v1
```

Important: localhost MCP config is intentionally **not bundled** into this public plugin package.
